{
  "name": "ridho-pay",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:local": "npm run build && wrangler pages dev dist/client --compatibility-date=2024-04-04 --kv KV",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "deploy:dev": "npm run build && wrangler dev"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.7",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.4",
    "tailwindcss": "^4.2.2",
    "wrangler": "^4.81.0"
  }
}
